export type WorkExperience = {
    _id: string;
    jobTitleId: string,
    company: string,
    description: string,
    startDate: string,
    endDate: string
    content?: string; // Thêm content vào đây

}